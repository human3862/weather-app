# Weather App 🌤️

- **[Демонстрация работы](https://weather-app-nine-taupe-91.vercel.app)**
- Современное веб-приложение для мониторинга погоды в реальном времени. Позволяет пользователям мгновенно получать данные о погодных условиях в любой точке мира с автоматическим сохранением истории запросов.

## 🚀 Стек технологий

Проект построен на актуальном и производительном стеке:

- **React 18** — использование функциональных компонентов и современных хуков.
- **Zustand** — легкое и быстрое управление глобальным состоянием (вместо Redux Toolkit).
- **TypeScript** — строгая типизация всего приложения для стабильности кода.
- **Vite** — инструмент сборки нового поколения с мгновенной перезагрузкой (HMR).
- **Tailwind CSS** — современный фреймворк для быстрой адаптивной верстки.
- **Fetch API** — взаимодействие с внешними погодными сервисами.

## ✨ Реализованный функционал

### 🧠 Управление состоянием (Zustand)
- **Global Store:** Вся бизнес-логика (поиск, фильтрация, хранение данных) вынесена из компонентов в централизованное хранилище.
- **Persistence (LocalStorage):** Благодаря middleware `persist`, история вашего поиска и последние данные не пропадают при перезагрузке страницы.
- **Оптимизация рендеров:** Использование селекторов позволяет компонентам обновляться только тогда, когда меняются конкретные нужные им данные.

### 🌤️ Работа с погодой
- **Умный поиск:** Получение актуальной температуры, влажности, скорости ветра и прогноза на несколько дней.
- **История запросов:** Реализован список последних 5 уникальных городов. При повторном поиске дубликаты автоматически фильтруются.
- **Интерактивный прогноз:** Переключение между карточками дней для детального просмотра погоды на выбранную дату.

### 🛠️ Технические особенности
- **TS Config Architecture:** Настроена сложная конфигурация TypeScript с использованием `references` для разделения сред исполнения (Browser vs Node).
- **Error Handling:** Реализована обработка ошибок сети и некорректных ответов от API.
- **Mobile First:** Полная адаптивность интерфейса под любые размеры экранов.

## 📦 Запуск проекта

1. Склонируйте репозиторий:
   ```bash
   git clone https://github.com
2. Установите зависимости:
   ```bash
   npm install
3. Запустите проект в режиме разработки:
   ```bash
   npm run dev
   
      
